# Screenshots

Drop the following PNGs in this directory to wire them into the main README:

| Filename | What to capture |
|----------|-----------------|
| `topics-list.png` | `/admin/blog-topic-resource/blog-topics` — the list view with at least 3 rows in different states (Queued, Drafting, Drafted) |
| `topic-edit.png` | A single topic in edit mode, showing `auto_publish` + `target_status` |
| `prompts.png` | `/admin/stringer-prompt-resource/stringer-prompts` — list view showing the two seeded rows (`draft`, `translate`) |
| `prompt-edit.png` | The `draft` prompt in edit mode, scrolled to show the few-shot example |
| `content-fields.png` | `/admin/stringer-content-field-resource/stringer-content-fields` — list view showing the 5 baseline fields |
| `settings.png` | `/admin/manage-stringer-settings` — the form with the voice card and cron fields visible |
| `telegram-flow.png` | A Telegram screenshot: `/generate <hint>` → ack reply → `Draft #N is ready — <url>` notification |

Recommended capture: ~1600px wide, light theme, with realistic data (not "Lorem ipsum"). PNG/WEBP preferred.
